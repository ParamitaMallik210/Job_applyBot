# Resume folder

Drop your resume PDF here and name it exactly **`resume.pdf`** (or change `resume.path` in `config.yml`).

The bot parses this file once per run and matches it against every job description.

> ⚠️ Make sure the GitHub repo is **private** — your resume is committed to it.
